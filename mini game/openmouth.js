function getAnswer () {
    var hiddenAnswer = Math.floor(Math.random()* 100);
    console.log (hiddenAnswer);

    }

function guess() {        
        var pa = playerAnswer.value;
        console.log(pa);
    }